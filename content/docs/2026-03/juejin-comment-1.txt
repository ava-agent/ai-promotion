哈哈，这个思路挺有意思的 👍

之前做项目的时候也折腾过监控，最开始直接用 sentry，虽然方便但是一些业务自定义的指标不好采集。后来也想自己搞一套，结果卡在 sendBeacon 的兼容性问题上了，特别是跨域的时候各种坑。

看到你用 Blob 包装 JSON 的方式，学习了！我之前都没注意到可以这样处理 Content-Type。

另外那个 requestIdleCallback 的兜底方案也挺实用的，我们项目还要支持 IE11（别问为什么 😂），确实得考虑 setTimeout 降级。

想请教一下，你们实际使用中，上报的数据量大概有多大？我们之前担心批量上报会积压太多导致内存问题，你是怎么处理的？